wget https://sqlite.org/2021/sqlite-autoconf-3350300.tar.gz

tar xvfz sqlite-autoconf-3350300.tar.gz

cd sqlite-autoconf-3350300

./configure --prefix=/usr/local
make
sudo make install

sudo ldconfig

sqlite3 --version